<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed to reach the Spring Boot backend over the network -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Needed for the barcode scanner when adding books and processing returns -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomeLibraryAdmin">

        <!-- Login is the true entry point of the app -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.HomeLibraryAdmin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- One-time admin account creation - reached via a link on the login screen -->
        <activity
            android:name=".ui.auth.BootstrapAdminActivity"
            android:exported="false"
            android:theme="@style/Theme.HomeLibraryAdmin"/>

        <!-- Main app shell - hosts the fragments and floating bottom nav -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.HomeLibraryAdmin"/>

    </application>

</manifest>